import 'package:flutter/material.dart';

class OnboradingInfoModel {
  String title, desc, image;
  Color color;
  OnboradingInfoModel(
      {required this.title,
      required this.desc,
      required this.image,
      required this.color});
}
